#pragma once
#include "precompile.h"

class SolarSystem {
public:
    void systemTextures();
    void displaySystem();
    void buttonHandler();
    void updateBackground();

private:
    Font font;
};